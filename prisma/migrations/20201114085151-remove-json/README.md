# Migration `20201114085151-remove-json`

This migration has been generated by Phat Tran at 11/14/2020, 3:51:51 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Item" ALTER COLUMN "images" DROP NOT NULL,
ALTER COLUMN "images" SET DATA TYPE text ,
ALTER COLUMN "checkBeforeRentDocuments" DROP NOT NULL,
ALTER COLUMN "checkBeforeRentDocuments" SET DATA TYPE text ,
ALTER COLUMN "keepWhileRentingDocuments" DROP NOT NULL,
ALTER COLUMN "keepWhileRentingDocuments" SET DATA TYPE text 

ALTER TABLE "public"."RentingItemRequestComment" ALTER COLUMN "files" SET DATA TYPE text []

ALTER TABLE "public"."User" ALTER COLUMN "avatarImage" SET DATA TYPE text ,
ALTER COLUMN "coverImage" SET DATA TYPE text 
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201114070011-final-items..20201114085151-remove-json
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -20,10 +20,10 @@
   email         String      @unique
   passwordHash  String
   name          String?
   bio           String?
-  avatarImage   Json? // { id, url }
-  coverImage    Json? // { id, url }
+  avatarImage   String? // { id, url }
+  coverImage    String? // { id, url }
   role          UserRole[]
 }
 model Area {
@@ -87,12 +87,12 @@
   slug                    String          @unique
   description             String?
   categories              Category[]      @relation(references: [id])
   areas                   Area[]          @relation(references: [id])
-  images                  Json[]
+  images                  String?
   roughAddress            String?
-  checkBeforeRentDocuments         Json[]
-  keepWhileRentingDocuments        Json[]
+  checkBeforeRentDocuments         String?
+  keepWhileRentingDocuments        String?
   unavailableForRentDays  DateTime[]
   currentOriginalPrice    Float?
   sellPrice               Float?
   rentPricePerDay         Float?
@@ -203,9 +203,9 @@
   id                      String          @id @default(uuid())
   rentingItem           Item @relation(fields: [itemId], references: [id])
   itemId                String
   content             String?
-  files                 Json[]
+  files                 String[]
   isLocked              Boolean?        @default(false)
   lockedBy               String
   isDisabled              Boolean?        @default(false)
   isDeleted               Boolean?        @default(false)
```


