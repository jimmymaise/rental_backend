# Migration `20201107141035-update-user`

This migration has been generated by Phat Tran at 11/7/2020, 9:10:36 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."FileStorage" DROP CONSTRAINT "FileStorage_fileStorageId_fkey"

ALTER TABLE "public"."FileStorage" DROP CONSTRAINT "FileStorage_itemId_fkey"

ALTER TABLE "public"."FileStorage" DROP COLUMN "itemId",
DROP COLUMN "fileStorageId",
ADD COLUMN "thumbnailMap" jsonb   

ALTER TABLE "public"."Item" ADD COLUMN "images" jsonb []  

ALTER TABLE "public"."User" ADD COLUMN "passwordHash" text   NOT NULL ,
ADD COLUMN "avatarImage" jsonb   ,
ADD COLUMN "coverImage" jsonb   ,
ALTER COLUMN "bio" DROP NOT NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201107081736-add-more-tables..20201107141035-update-user
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -15,13 +15,16 @@
   User
 }
 model User {
-  id      String      @id @default(uuid())
-  email   String      @unique
-  name    String?
-  bio     String
-  role    UserRole[]
+  id            String      @id @default(uuid())
+  email         String      @unique
+  passwordHash  String
+  name          String?
+  bio           String?
+  avatarImage   Json? // { id, url }
+  coverImage    Json? // { id, url }
+  role          UserRole[]
 }
 model Area {
   id            String      @id @default(uuid())
@@ -60,9 +63,9 @@
   contentType       String
   createdBy         String
   createdDate       DateTime      @default(now())
   isDeleted         Boolean?      @default(false)
-  thumbnail         FileStorage?
+  thumbnailMap      Json? // { 300: { id, url, name, bucketName, folderName } }
 }
 enum ItemStatus {
   Draft
@@ -77,9 +80,9 @@
   slug                    String          @unique
   description             String?
   categories              Category[]      @relation(references: [id])
   areas                   Area[]          @relation(references: [id])
-  images                  FileStorage[]
+  images                  Json[]
   unavailableForRentDays  DateTime[]
   currentOriginalPrice    Float?
   sellPrice               Float?
   rentPricePerDay         Float?
```


