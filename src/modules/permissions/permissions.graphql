type PermissionInfo {
  name: String
  description: String
  createdDate: Float
  updatedDate: Float
}

type PermissionsResult {
  items: [PermissionInfo]
}

type Query {
  getOrgAvailablePermissions: PermissionsResult
}
