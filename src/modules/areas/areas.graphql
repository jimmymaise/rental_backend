type Area {
  id: String!
  name: String!
  slug: String!
  parentAreaId: String
  latitude: Float
  longitude: Float
  isDisabled: Boolean
}

type Query {
  getAllAvailableAreas(isDisabled: Boolean): [Area]
}
