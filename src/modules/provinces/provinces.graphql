type Query {
  getProvincesByCountryId(countryId: ID!): [Province]
}

type Province {
  id: String!
  countryId: String!
  countrySlug: String!
  name: String
  slug: String
  description: String
  latitude: Float
  longitude: Float
  country: Country
}
