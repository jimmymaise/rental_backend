type DeleteItem {
  id: String!
}

enum UpdateType {
  Connect
  Disconnect
  Set
}

input BaseOffsetPagingInput {
  pageSize: Int
  offset: Int
  orderBy: JSON
}
input BaseCursorPagingInput {
  pageSize: Int
  cursor: String
}
